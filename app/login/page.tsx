'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function Login(){ const r=useRouter(); const [error,setError]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})}); const d=await res.json(); if(!res.ok)return setError(d.error); r.push(d.role==='admin'?'/admin':'/'); r.refresh();} return <main className="form-page"><form className="form-card" onSubmit={submit}><div className="brand"><span>JP</span>JastipPro</div><h1>Masuk</h1><p>Kelola pesanan dan akun kamu.</p><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary">Masuk</button><a href="/">← Kembali ke toko</a></form></main> }
