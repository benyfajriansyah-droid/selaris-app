'use client'; import { useEffect,useState } from 'react';
type Item={id:string|number;name:string;price:number;fee:number;qty:number};
export default function Checkout(){const [items,setItems]=useState<Item[]>([]);const [done,setDone]=useState<any>(null);const [err,setErr]=useState('');useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem('jp_cart')||'[]'))}catch{}},[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setErr('');const f=new FormData(e.currentTarget);const body={customer_name:f.get('name'),customer_email:f.get('email'),customer_phone:f.get('phone'),address:{line:f.get('address'),city:f.get('city'),postal_code:f.get('postal')},items:items.map(i=>({id:i.id,qty:i.qty}))};const r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)return setErr(d.error||'Checkout gagal');setDone(d);localStorage.removeItem('jp_cart');}
if(done)return <main className="form-page"><div className="form-card"><h1>Order berhasil 🎉</h1><p>No. order: <b>{done.order_no}</b></p><p>Pesanan menunggu pembayaran.</p><a className="primary" href="/track">Tracking order</a><a href="/">Kembali ke toko</a></div></main>;
return <main className="form-page"><form className="form-card wide" onSubmit={submit}><div className="brand"><span>JP</span>Checkout</div><h1>Data penerima</h1><p>{items.length} jenis barang di keranjang.</p><label>Nama lengkap<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>No. WhatsApp<input name="phone" required/></label><label>Alamat<textarea name="address" required/></label><div className="form-row"><label>Kota<input name="city" required/></label><label>Kode pos<input name="postal" required/></label></div>{err&&<div className="form-error">{err}</div>}<button className="primary" disabled={!items.length}>Buat pesanan</button><a href="/">← Kembali ke toko</a></form></main>}
